.origin 0xff

.db 0x1, 0x2,0x3,0x4...0xa

//for(int i = 0; i < 10; i++)
//	std::cout << arr[i]<<"\n";

Loop:

	LOAD r2,0 // i = 0
	LOAD r0,0xff //r0 = 0xff adres

	LOAD r1,@r0  //r1 = ram[r0]
	STR @FRAME, r1 //ram[FRAME] = r1

	ADD r0,1	//adres++
	CMP r0,10	//r0 == 10
	JCF Loop	//r0 == 10 => false pc = Loop